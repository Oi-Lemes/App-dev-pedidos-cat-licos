{
  "name": "area-de-membros-root",
  "version": "1.0.0",
  "scripts": {
    "build": "cd backend && npm install && npx prisma generate",
    "start": "cd backend && node prisma/seed_catholic.js && node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {}
}